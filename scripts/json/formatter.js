// json formatter.txt 

// encodes a string to ASCII and prints to html so it can be copied into JSON object LOL

let html = `<p><span class="drop-cap">O</span>ops. This city is not yet occupied. Check back next week.</p>`;

// let asciiArray = [];
// for (i = 0; i < html.length; i++) {
//   let code = html.codePointAt(i)
//   asciiArray.push(code);
// }

// const body = document.querySelector("body");
// body.innerHTML = asciiArray;
